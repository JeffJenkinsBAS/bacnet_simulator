{
  "ollama_host": "http://localhost:11434",
  "default_model": "llama3.1",
  "timeout_seconds": 60,
  "_note": "Not yet wired to a Settings GUI -- see PHASE6_REVIEW.md open question #5 (which model(s) are actually available on this laptop's Ollama install). Edit this file directly for now; app/main.py should load it once the LLM Console panel exists."
}
